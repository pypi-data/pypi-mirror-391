[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lcp-chart-mcp"
version = "0.1.1"
description = "一個 MCP 伺服器，提供圖表生成工具"
dependencies = ["mcp", "httpx"]

[project.scripts]
lcp-chart-mcp = "lcp_chart_mcp.server:main"


[tool.hatch.build.targets.wheel]
packages = ["src/lcp_chart_mcp"]