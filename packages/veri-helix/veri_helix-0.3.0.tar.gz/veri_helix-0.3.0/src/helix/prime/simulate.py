"""Prime editing outcome simulator (placeholder)."""
from __future__ import annotations

from typing import Dict


def simulate_prime_outcomes(
    seq: str,
    edit_spec: Dict[str, object],
    design: Dict[str, object],
    priors: Dict[str, float],
    *,
    seed: int | None = None,
) -> Dict[str, object]:
    raise NotImplementedError("Prime editing simulator will be implemented in a later milestone.")
