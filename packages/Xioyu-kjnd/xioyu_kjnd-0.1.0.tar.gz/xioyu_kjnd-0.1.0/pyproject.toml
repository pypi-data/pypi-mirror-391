[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Xioyu_kjnd"
version = "0.1.0"
description = "Demo package for PyPI publish"
authors = [{ name = "Brunda Adiga", email = "brunda.v@adaglobal.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.6"
