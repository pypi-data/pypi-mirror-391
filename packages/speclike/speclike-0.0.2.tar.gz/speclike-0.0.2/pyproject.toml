[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "speclike"
version = "0.0.2"
description = "Partial support library for structured testing"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "minoru_jp" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
keywords = [
    "spec",
    "test",
    "marker",
    "pytest",
    "structured",
    "dbc",
    "contract",
    "asyncio",
]

[project.urls]
Homepage = "https://github.com/minoru-jp/speclike"
Source = "https://github.com/minoru-jp/speclike"
Issues = "https://github.com/minoru-jp/speclike/issues"

[project.optional-dependencies]
pytest = ["pytest>=7.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["speclike*"]

[project.entry-points.pytest11]
speclike = "speclike.plugin"
