"""Top-level package initializer for the repository.

Exposes the internal modules under the `src.*` package path so that tests and
tools can reliably import modules like `src.workflows.supervisor_workflow`.
"""

# Optionally expose convenience imports (kept minimal to avoid side effects)
