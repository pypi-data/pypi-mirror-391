[project]
name = "wlmclear"
version = "1.0.0"
description = "WindowsMacLiux Clear, clears the console in all 3 major operating systems with just the simple command: clean"
authors = [
    {name = "RottenZorro",email = "dischak.kh@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "wlmclear", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
