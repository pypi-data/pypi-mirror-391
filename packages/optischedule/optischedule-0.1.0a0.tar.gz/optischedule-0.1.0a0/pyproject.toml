[project]
name = "optischedule"
version = "0.1.0a"
description = "Optimized scheduling."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []


[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"


[tool.uv.build-backend]
module-name = "optischedule"
module-root = ""
