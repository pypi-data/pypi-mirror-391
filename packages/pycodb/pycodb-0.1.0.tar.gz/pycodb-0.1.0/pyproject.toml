[project]
name = "pycodb"
version = "0.1.0"
description = "A simple module for working with NocoDB"
authors = [
    {name = "Helpmap",email = "helpmap.online@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.6"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
