services:
  siga:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8969:8969"
    volumes:
      - ./siga_mcp:/app/siga_mcp
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
