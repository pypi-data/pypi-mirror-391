{
  "schemaVersion": "1.0.0",
  "id": "parser_json_diagnostics",
  "type": "parser",
  "description": "Parse JSON payloads into RawDiagnostic entries using catalog mappings.",
  "implementation": "pyqa.catalog.strategies.parser_json_diagnostics",
  "config": {
    "path": {
      "type": "string",
      "description": "Optional dotted/JSONPath expression selecting diagnostic entries.",
      "required": false
    },
    "inputFormat": {
      "type": "string",
      "description": "Loader hint describing the stdout format (json, json-lines).",
      "required": false
    },
    "mappings": {
      "type": "object",
      "description": "Field mapping configuration describing how to populate diagnostic fields.",
      "required": true
    }
  }
}
