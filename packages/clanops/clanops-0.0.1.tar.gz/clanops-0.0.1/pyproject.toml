[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clanops"
version = "0.0.1"
description = "ClanOps namespace placeholder"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "ClanOps LLC" }]
license = { text = "UNLICENSED" }
