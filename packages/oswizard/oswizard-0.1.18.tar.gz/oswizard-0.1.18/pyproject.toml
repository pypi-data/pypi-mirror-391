[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]

name = "oswizard"
version = "0.1.18"
description = "Automated OS provisioning and installation orchestrator"
authors = [{ name="OSWizard Dev Team", email="dev@oswiz.io" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "typer>=0.12.3",
    "jinja2>=3.1.4",
    "rich>=13.7.1",
    "pydantic>=2.8.2",
    "pyyaml>=6.0.2",
    "tenacity>=9.0.0",
]

[tool.setuptools.packages.find]
include = ["oswizard*"]

[project.scripts]
osw = "oswizard.cli:cli"

# DUPLICATE_PROJECT_HEADER_REMOVED
# MOVED_README = { file = "README.md", content-type = "text/markdown" }
