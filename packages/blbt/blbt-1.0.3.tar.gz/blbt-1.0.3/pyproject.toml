[project]
name = "blbt"
version = "1.0.3"
description = "Bambu Lab Boosts Tool - Calculate the gift card value of Makerworld boosts with live exchange rates."
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "thefilipcom4607", email = "fm@thefilip.com" }
]
dependencies = [
  "requests>=2.31.0",
  "colorama>=0.4.6"
]

[project.urls]
Homepage = "https://github.com/thefilipcom4607/blbt"

[project.scripts]
blbt = "blbt.__main__:main"

