[project]
name = "rm_measurement"
version = "1.0.0"
authors = [
  { name="Robert Mende"},
]
description = "Defines the Measurement Entity"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"