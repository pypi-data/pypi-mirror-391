[project]
name = "mcp-sqlite-server-d"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.21.1",
    "twine>=6.2.0",
]

[project.scripts]
mcp-sqlite-server-d = "mcp_sqlite_server_d:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
