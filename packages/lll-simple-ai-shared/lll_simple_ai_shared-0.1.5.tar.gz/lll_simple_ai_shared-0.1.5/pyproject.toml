[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lll-simple-ai-shared"
version = "0.1.5"
description = "数据类型定义"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = {find = {where = ["."], include = ["lll_simple_ai_shared", "lll_simple_ai_shared.*"]}}