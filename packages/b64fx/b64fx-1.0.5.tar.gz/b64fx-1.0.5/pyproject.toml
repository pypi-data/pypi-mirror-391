[build-system]
requires = ["setuptools>=42", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "b64fx"
version = "1.0.5"
description = "better than original base64"
authors = [{name="Anonymous", email="m00263277@gmail.com"}]
readme = "README.md"
"requires-python" = ">=3.8"
dependencies = ["Cython"]