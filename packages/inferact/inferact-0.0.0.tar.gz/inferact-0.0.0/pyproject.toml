[project]
name = "inferact"
version = "0.0.0"
description = "Reserved"
authors = [
    { name = "Woosuk Kwon", email = "woosuk.kwon@berkeley.edu" }
]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
