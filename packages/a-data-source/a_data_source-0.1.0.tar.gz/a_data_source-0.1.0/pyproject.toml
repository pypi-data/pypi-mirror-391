[project]
name = "a-data-source"
version = "0.1.0"
description = "Data sources is module for simplifying data fetching and processing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
drf = [
    "djangorestframework>=3.14.0",
]
django = [
    "django>=4.2.0",
]
all = [
    "djangorestframework>=3.14.0",
    "django>=4.2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
]
