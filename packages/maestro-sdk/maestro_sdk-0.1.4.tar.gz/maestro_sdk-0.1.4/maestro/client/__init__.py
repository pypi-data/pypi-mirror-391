"""
Python REST client.

It supports following Maestro APIs:
- push a workflow yaml or dsl object
- validate a workflow yaml or dsl object
- start a workflow instance with input runtime parameters
- stop a workflow instance
- watch/monitor a workflow instance execution
- run a workflow by starting it and monitoring it until it completes
- get workflow instance
- get step instance
"""
