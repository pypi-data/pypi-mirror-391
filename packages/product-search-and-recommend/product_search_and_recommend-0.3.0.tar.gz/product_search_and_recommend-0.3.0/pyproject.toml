[project]
name = "product_search_and_recommend"
version = "0.3.0"
description = "MCP server for product search and recommendation"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "pydantic>=2.0.0",
]

[project.scripts]
product_search_and_recommend = "product_search_and_recommend:main"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_server_product"]