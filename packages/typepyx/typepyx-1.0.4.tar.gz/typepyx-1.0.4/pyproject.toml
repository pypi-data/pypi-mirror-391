[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "typepyx"
version = "1.0.4"
description = "Custom type system for Python similar to TypeScript"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Nambooproo" }]
requires-python = ">=3.8"
