runtime: container

startCommand:
  type: http
  configSchema:
    type: object
    properties:
      debug:
        type: boolean
        title: "Debug Mode"
        description: "Enable debug logging"
        default: true

build:
  dockerfile: "Dockerfile.streamable-http"
  dockerBuildPath: "."
