services:
  {project_name}:
    image: {project_name}
    container_name: {project_name}
    restart: always
    ports:
      - "{port}:443"

    volumes:
      - ./files:/fasttask/files

    environment:
      - NODE_TYPE=single_node
