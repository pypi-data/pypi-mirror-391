# setup.py
from setuptools import setup, find_packages

setup(
    name="akimgtransmitter",
    version="0.1.0",
    description="Simple image/frame transmitter that saves latest frame to a folder.",
    author="Akshay ajit bhawar",
    packages=find_packages(),
    python_requires=">=3.7",
)
