[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "py-bcp-utils"
version = "0.1.0"
authors = [
  { name="João Afonso", email="contact.joaoafonso@gmail.com" },
]
description = "A utility to run BCP bulk inserts from a Pandas DataFrame."
readme = "README.md"
requires-python = ">=3.8"

# This is the modern way to declare a license
license = "MIT"

classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Database",
]
dependencies = [
    "pandas>=1.0.0",
]

[project.urls]