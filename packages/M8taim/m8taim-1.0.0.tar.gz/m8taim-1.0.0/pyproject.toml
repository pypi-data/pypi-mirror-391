[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "M8taim"
version = "1.0.0"
description = "Advanced time-based protection system for Python tools"
authors = [{name = "MERO", email = "contact@example.com"}]
requires-python = ">=2.7"
dependencies = [
    "setuptools>=44.1.1",
]
