import type { ActorHeaderInfo } from "@bovine/muck-out-types";

function determineAvatarLink(actor: ActorHeaderInfo) {
  if (actor.htmlUrl) {
    return actor.htmlUrl;
  }
  return actor.id;
}

export { determineAvatarLink };
