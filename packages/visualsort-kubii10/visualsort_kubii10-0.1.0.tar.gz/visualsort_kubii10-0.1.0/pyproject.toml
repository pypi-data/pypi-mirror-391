[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "visualsort-kubii10"
version = "0.1.0"
description = "Library to visualise sorting"
authors = [
    { name="Kubii10", email="supcod34@email.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.0.0"
]
