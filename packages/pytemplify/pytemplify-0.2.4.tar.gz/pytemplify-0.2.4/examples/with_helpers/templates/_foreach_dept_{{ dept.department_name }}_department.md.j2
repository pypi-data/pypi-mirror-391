# {{ dept.department_name }} Department - Detailed Report

**Manager:** {{ dept.manager_name }}

---

## Overview

{{ dept.department_summary }}

## Financial Information

| Item | Amount |
|------|--------|
| Department Budget | ${{ "{:,}".format(dept._data.budget) }} |
| Total Salaries | ${{ "{:,}".format(dept.department_salary_expense) }} |
| Average Salary | ${{ "{:,}".format(dept.average_salary | int) }} |
| Budget per Employee | ${{ "{:,}".format(dept.budget_per_employee | int) }} |
| Remaining Budget | ${{ "{:,}".format(dept._data.budget - dept.department_salary_expense) }} |

{% if dept.is_large_department -%}
## 🏢 Large Department Status

This is classified as a large department with {{ dept.employee_count }} employees.

{% endif -%}

## Team Structure

**Employee Count:** {{ dept.employee_count }}
**Highest Paid:** {{ dept.highest_paid_employee }}

### Team Members

{% for emp in dept.employees -%}
#### {{ loop.index }}. {{ emp.name }}

- **Title:** {{ emp.title }}
- **Seniority:** {{ emp.seniority_level }}{% if emp.is_senior %} ⭐{% endif %}
- **Salary:** {{ emp.formatted_salary }}
- **Annual Cost:** {{ emp.formatted_annual_cost }} (including benefits)
- **Years of Service:** {{ emp.years_of_service }}
- **Started:** {{ emp._data.start_date }}

{{ emp.employee_summary }}

{% endfor %}

---

## Department Metrics

### Salary Distribution

{% for emp in dept.employees | sort(attribute='_data.salary', reverse=true) -%}
{{ loop.index }}. {{ emp.name }}: {{ emp.formatted_salary }}
{% endfor %}

### Seniority Distribution

{% set senior = dept.employees | selectattr('is_senior', 'equalto', true) | list -%}
{% set non_senior = dept.employees | selectattr('is_senior', 'equalto', false) | list -%}

- **Senior Level:** {{ senior | length }} ({{ "%.0f" | format(senior | length / dept.employee_count * 100) }}%)
- **Other Levels:** {{ non_senior | length }} ({{ "%.0f" | format(non_senior | length / dept.employee_count * 100) }}%)

---

*Generated by PyTemplify with Data Helpers*
