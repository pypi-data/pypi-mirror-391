"""Debug utilities for {{ service.name }}

This file contains debugging and diagnostic utilities.
"""

import logging
from typing import Dict, Any

logger = logging.getLogger(__name__)


class {{ service.name | title | replace('-', '') }}Debug:
    """Debug utilities for {{ service.name }}."""

    @staticmethod
    def get_status() -> Dict[str, Any]:
        """Get current status of {{ service.name }}."""
        return {
            "service": "{{ service.name }}",
            "category": "{{ service.category }}",
            "port": {{ service.port }},
            "description": "{{ service.description }}"
        }

    @staticmethod
    def dump_config() -> Dict[str, Any]:
        """Dump configuration for debugging."""
        logger.debug(f"Dumping config for {{ service.name }}")
        return {
            "name": "{{ service.name }}",
            "port": {{ service.port }},
            "category": "{{ service.category }}"
        }

    @staticmethod
    def run_diagnostics() -> bool:
        """Run diagnostic checks."""
        logger.info(f"Running diagnostics for {{ service.name }}")
        # TODO: Implement diagnostic checks
        return True
