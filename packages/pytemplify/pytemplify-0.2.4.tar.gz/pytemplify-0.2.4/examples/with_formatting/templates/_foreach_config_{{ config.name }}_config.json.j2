{
  "{{ config.name }}": {
    "version": "{{ globals.version }}",
    "project": "{{ globals.project }}",
    "description": "{{ config.name | title() }} configuration",
    "settings": {{ config.settings | tojson(indent=2) }},
    "generated_at": "{{ '2024-01-01 12:00:00' }}",
    "metadata": {
      "auto_generated": true,
      "formatter": "prettier",
      "template_version": "{{ globals.version }}"
    }
    // MANUAL SECTION START: custom_config
    // Add your custom configuration here
    // This section will be preserved during formatting
    // "custom_setting": "your_value"
    // MANUAL SECTION END
  }
}