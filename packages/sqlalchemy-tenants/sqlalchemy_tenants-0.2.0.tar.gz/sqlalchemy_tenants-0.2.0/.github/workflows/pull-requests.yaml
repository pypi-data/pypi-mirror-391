name: Pull Request Pipeline

on:
  workflow_dispatch:
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - ".github/**"
      - "*.md"
      - "docs/**"

jobs:
  CI:
    uses: ./.github/workflows/.ci-template.yaml
