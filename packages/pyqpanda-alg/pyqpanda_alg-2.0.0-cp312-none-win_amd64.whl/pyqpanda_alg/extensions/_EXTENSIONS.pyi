def QAdder(*args, **kwargs):
    """QAdder(adder1: list[int], adder2: list[int], c: int, is_carry: int) -> QPanda3::QCircuit

    Quantum adder.
    """
def QAdderIgnoreCarry(*args, **kwargs):
    """QAdderIgnoreCarry(adder1: list[int], adder2: list[int], c: int) -> QPanda3::QCircuit

    Quantum adder ignore carry.
    """
def amplitude_encode(*args, **kwargs):
    """amplitude_encode(*args, **kwargs)
    Overloaded function.

    1. amplitude_encode(qubits: list[int], full_cur_vec: list[complex]) -> QPanda3::QCircuit

    Amplitude Encode.

    2. amplitude_encode(q: list[int], data: list[float], b_need_check_normalization: bool = True) -> QPanda3::QCircuit

    Amplitude Encode.
    """
