[project]
name = "pipe-save"
version = "0.1.0"
description = "pipe-save: a more structured version of tee"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
]
