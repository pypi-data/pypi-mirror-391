# These env vars will be available to use in the docker compose file.
# This file itself contains the prod config. when running locally for dev/test use the .env.dev file.

# sets the base path from which config is loaded.
CONFIG_BASE_PATH=/etc/easyrunner