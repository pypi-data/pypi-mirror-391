# These env vars will be available to use in the docker compose file during local dev
# 'podman-compose -f .templates/docker-compose/docker-compose-host.yaml up --env-file .env.dev up -d'

# sets the base path from which config is loaded.
CONFIG_BASE_PATH=..