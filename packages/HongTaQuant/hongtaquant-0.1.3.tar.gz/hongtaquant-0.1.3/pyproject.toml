[project]
name = "HongTaQuant"
version = "0.1.3"
description = "HongTaQuant是基于红塔证券量化衍生出来的一套Python框架，对外以Python库的形式提供策略交易所需要的行情和相关的API接口。"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name="HongTa Stock Quant Team"}]
keywords = ["finance", "data", "sdk", "kafka", "http", "streaming"]
dependencies = [
  "confluent-kafka>=2.12.1",
  "httpx>=0.25.0",
  "pydantic>=2.0.0",
  "pydantic-settings>=2.0.0",
  "tenacity>=8.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.target.wheel]
packages = ["src/HongTaQuant"]

[project.optional-dependencies]
dev = [
  "pytest>=7.4.0",
  "pytest-asyncio>=0.21.0",
  "pytest-cov>=4.1.0",
  "pytest-mock>=3.11.0",
  "ruff>=0.1.0",
  "mypy>=1.5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py312"
line-length = 100
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true