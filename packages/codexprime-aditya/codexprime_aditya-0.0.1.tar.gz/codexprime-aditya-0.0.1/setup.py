from setuptools import setup, find_packages

setup(
    name="codexprime-aditya",  # !! UNIQUE NAAM CHUNEIN !!
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        "requests",  # Isse 'requests' library ki zaroorat hai
    ],
)