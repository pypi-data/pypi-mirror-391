# ============================================
# Gemini CLI Configuration
# ============================================

# This MCP server uses the Gemini CLI instead of API keys
# NO API KEY NEEDED - Uses your Gemini CLI credentials

# Prerequisites:
# 1. Install Gemini CLI: npm install -g @google/gemini-cli
# 2. Authenticate: Run `gemini` and follow the prompts
# 3. Your credentials are cached automatically

# Model to use (default: auto)
# "auto" - CLI automatically selects best model (Pro for complex tasks, Flash for simple)
# Available models: auto, gemini-2.0-flash, gemini-1.5-pro, gemini-2.5-flash-lite, etc.
GEMINI_MODEL=auto

# ============================================
# Workflow Directories
# ============================================

# Where to save generated specifications
DEFAULT_SPEC_DIR=./specs

# Where to save code reviews
DEFAULT_REVIEW_DIR=./reviews

# Where to cache context
DEFAULT_CONTEXT_DIR=./.workflow-context

# ============================================
# Advanced Configuration (Optional)
# ============================================

# Override default Gemini CLI path (if not in PATH)
# GEMINI_CLI_PATH=/usr/local/bin/gemini

# ============================================
# NOTES:
# ============================================
# - No API key required! Uses Gemini CLI credentials
# - Zero additional API costs (uses your Code Assist subscription)
# - Authenticate once with `gemini` command, credentials are cached
# - To check if authenticated: run `gemini --version`
