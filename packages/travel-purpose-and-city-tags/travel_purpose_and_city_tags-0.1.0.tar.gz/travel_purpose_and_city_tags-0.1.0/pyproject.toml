[project]
name = "travel-purpose-and-city-tags"
version = "0.1.0"
description = "Travel purpose tagging library for city-specific travel behavior classification."
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Teyfik Öz", email = "teyfik@example.com"}]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
