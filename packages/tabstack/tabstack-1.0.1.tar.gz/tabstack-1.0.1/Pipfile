[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
tabstack-ai = {editable = true, path = "."}

[dev-packages]
pytest = ">=7.0.0"
mypy = ">=1.0.0"
black = ">=22.0.0"
ruff = ">=0.1.0"

[requires]
python_version = "3.8"
