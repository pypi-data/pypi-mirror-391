[build-system]
requires = ["setuptools>=70.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "roboactions"
dynamic = ["version"]
description = "Python client SDK for the RoboActions automation platform."
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
authors = [
  {name = "RoboActions", email = "eishan@roboactions.com"}
]
keywords = ["automation", "workflow", "sdk", "client", "roboactions"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules"
]
dependencies = [
    "requests>=2.32.3",
    "pydantic>=2.8.2",
    "msgpack>=1.0.0",
    "numpy>=1.24.0",
    "torch",
    "websocket-client>=1.8.0",
    "gymnasium>=0.29.0",
    "gymnasium[other]",
    "Pillow>=10.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.1",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.10"
]

[project.urls]
Homepage = "https://www.roboactions.com"
Documentation = "https://docs.roboactions.com/sdk/python"
Repository = "https://github.com/RoboActions/RoboActions"
Issues = "https://github.com/RoboActions/RoboActions/issues"

[tool.setuptools.dynamic]
version = {attr = "roboactions._version.__version__"}

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-ra -q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I"]
exclude = [".git", ".venv", "build", "dist"]

[tool.coverage.run]
branch = true
source = ["roboactions"]
