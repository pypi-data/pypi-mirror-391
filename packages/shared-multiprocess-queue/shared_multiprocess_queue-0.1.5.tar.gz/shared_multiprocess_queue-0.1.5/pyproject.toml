[project]
name = "shared-multiprocess-queue"
version = "0.1.5"
description = "A shared-memory based multiprocessing queue for cross-process communication"
readme = "README.md"
requires-python = ">=3.13.7"
authors = [
    {name = "Raymond Chastain", email = "RaymondLC92@protonmail.com"}
]
license = {text = "MIT"}
keywords = ["python", "package"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "cloudpickle>=3.0.0",
    "shared_memory_lock>=0.1.5"
]

[dependency-groups]
dev = [
    "pytest-check>=2.4.1",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-asyncio>=1.3.0",
    "mypy>=1.18.2",
]

[tool.uv.sources]
shared_multiprocess_queue = { workspace = true }

[tool.ruff]
line-length = 144

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["shared_multiprocess_queue"]

[tool.coverage.run]
branch = true

[tool.coverage.report]
exclude_also = [
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.mypy]
python_version = "3.13.7"
packages = ["shared_multiprocess_queue"]
strict = true