[project]
name = "osym.rpc"  # 包名（需确保在PyPI唯一）
version = "0.1.4"  # 初始版本号
authors = [
    { name = "nzturn", email = "nzturn@gmail.com" },
]
description = "Objects with SYMbolic expressions - RPC"  # 简短描述
readme = "README.md"  # 项目文档
requires-python = ">=3.8"  # 兼容的Python版本
keywords = ["symbolic", "dsl"]  # 可选：关键词便于搜索
classifiers = [  # 可选：PyPI分类标签
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
]

# 主依赖项
dependencies = ["osym.io"]