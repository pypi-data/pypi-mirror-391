[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tsa-core"
version = "0.0.1"
description = "Temporal State Abstraction Core — The OS for AI Agents"
authors = [{name = "Your Name", email = "you@yourdomain.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
keywords = ["ai", "temporal", "agents", "memory", "tsa", "state"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]

[project.urls]
Homepage = "https://tsacore.ai"
Repository = "https://github.com/tsa-core/tsa-core"