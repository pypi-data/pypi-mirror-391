[project]
name = "sockric"
version = "0.2.0"
description = "A Server/Client extension to the 'socket' library. Developed for personal use."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name="Luka Jacobsen" }]
license = { file="LICENSE" }

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"