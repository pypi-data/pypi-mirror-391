"""Version information for the IndusLabs plugin.

The version is defined in a separate module so that the build system can
read it without importing the rest of the package (which may require
additional dependencies).  The version is updated automatically by
`hatchling` during release.
"""

__version__ = "0.0.3"
