# Main package meson.build

# Install Python files
py.install_sources([
    '__init__.py',
    '__version__.py',
], subdir: 'basil_core')

# Process subdirectories
subdir('array_tools')
subdir('astro')
subdir('plots')
subdir('stats')
