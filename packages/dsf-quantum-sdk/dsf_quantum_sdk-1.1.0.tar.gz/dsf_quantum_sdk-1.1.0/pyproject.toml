# pyproject.toml
# Minimal configuration to comply with PEP 517/518
# All metadata remains in setup.py (functional configuration)

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
