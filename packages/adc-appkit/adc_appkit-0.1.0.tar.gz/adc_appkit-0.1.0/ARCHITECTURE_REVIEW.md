# Архитектурный обзор: Текущая vs Альтернативная реализация

## 📋 Оценка комментариев пользователя

### ✅ Все комментарии валидны и важны

#### **1. REQUEST-компонентам не проставляется app** ❌

**Проблема:** В текущей реализации `inst.set_app(self)` вызывается только для SINGLETON компонентов.

**Альтернатива:** ✅ Решено - `inst.set_app(self.app)` вызывается для всех компонентов в `_get_request_component_raw`.

#### **2. Авто-старт REQUEST через fire-and-forget** ❌

**Проблема:**

- Гонка: `comp.obj` может быть недоступен до завершения `start()`
- Исключения уходят в фон
- `asyncio.run()` внутри дескриптора опасен

**Альтернатива:** ✅ Решено - нет авто-старта в дескрипторе. Есть явный `await req.get(name)` или `await req.use(name)`.

#### **3. Запрет зависимостей между стратегиями** ❌

**Проблема:** Слишком жесткая проверка - запрещает REQUEST → SINGLETON.

**Альтернатива:** ✅ Решено - правильная логика:

- SINGLETON не может зависеть от REQUEST (защита от утечек)
- REQUEST может зависеть от SINGLETON (стандартная практика)

#### **4. Непоследовательная инъекция зависимостей** ❌

**Проблема:** Инъекция либо `.obj`, либо самого компонента - хрупко.

**Альтернатива:** ✅ Решено - всегда инъектируется `.obj` (готовый ресурс).

#### **5. REQUEST-компоненты не имеют статуса/стоп-порядка** ❌

**Проблема:** Остановка "как попало", нет учета зависимостей.

**Альтернатива:** ✅ Решено - есть `self._order` для отслеживания порядка создания и остановки в обратном порядке.

#### **6. Сбор дескрипторов только из **class**.**dict\*\*\*\* ❌

**Проблема:** Не учитывает наследование.

**Альтернатива:** ✅ Решено - сбор через `reversed(self.__class__.mro())`.

#### **7. Импорт из base_app внутри контейнера** ❌

**Проблема:** Жесткая связка и риски циклических импортов.

**Альтернатива:** ✅ Решено - инверсия зависимостей через `scope_getter: Callable[[], Optional[Dict]]`.

#### **8. Дескриптор возвращает компонент, а не интерфейс** ⚠️

**Проблема:** Неочевидно, когда нужно вызывать `start()`.

**Альтернатива:** ✅ Решено - есть два метода:

- `req.get(name)` - возвращает Component (нужен явный `await comp.start()` если нужно)
- `req.use(name)` - возвращает `.obj` после гарантированного старта

---

## 🎯 Оценка альтернативного варианта

### ✅ **Сильные стороны:**

1. **Правильная архитектура зависимостей**

   - Четкое разделение SINGLETON и REQUEST
   - Корректные правила зависимостей
   - Инверсия зависимостей через scope_getter

2. **RequestContext для параметров**

   - Можно прокидывать `client_id`, `user_id`, `request_id` и т.д.
   - Контекст автоматически инъектируется в конфиг

3. **Явный API**

   - `req.get(name)` - для получения компонента
   - `req.use(name)` - для получения объекта после старта
   - Нет скрытого поведения

4. **Правильный порядок остановки**

   - Учет зависимостей через `self._order`
   - Остановка в обратном порядке

5. **Поддержка наследования**

   - Сбор дескрипторов через MRO

6. **Гибкость конфигурации**
   - Поддержка override стратегии через `_strategy` в конфиге

### ⚠️ **Что нужно доработать:**

1. **RequestContext не реализован**

   - Нужно создать `adc_appkit/context.py` с dataclass

2. **Биндинг приложений как компоненты**

   - Не реализован
   - Нужно добавить возможность регистрации `BaseApp` как компонента

3. **Компонент дескриптор не использует ensure_request**
   - `ComponentDescriptor.__get__` вызывает `get_component`, который возвращает "сырой" компонент
   - Для REQUEST компонентов нужно вызывать `ensure_request` или требовать явный `req.get()`

---

## 🚀 Рекомендации

### **Вариант 1: Перейти на альтернативный вариант** (рекомендуется)

**Преимущества:**

- ✅ Решены все проблемы из комментариев
- ✅ Правильная архитектура
- ✅ Готовность к расширению (RequestContext, биндинг приложений)

**Недостатки:**

- ⚠️ Нужно создать RequestContext
- ⚠️ Нужно добавить биндинг приложений

### **Вариант 2: Исправить текущую реализацию**

**Недостатки:**

- ❌ Много точечных исправлений
- ❌ Останутся архитектурные проблемы
- ❌ Сложнее добавлять новые фичи

---

## 📝 План реализации (если выбираем альтернативу)

### **Шаг 1: Создать RequestContext**

```python
# adc_appkit/context.py
from dataclasses import dataclass, field
from typing import Optional, Dict, Any

@dataclass
class RequestContext:
    """Контекст текущего запроса для передачи в REQUEST компоненты."""
    request_id: Optional[str] = None
    user_id: Optional[str] = None
    client_id: Optional[str] = None
    tenant_id: Optional[str] = None
    # Произвольные дополнительные данные
    extra: Dict[str, Any] = field(default_factory=dict)
```

### **Шаг 2: Добавить биндинг приложений**

```python
# В BaseApp добавить метод:
def register_app_component(self, name: ComponentName, app: "BaseApp") -> None:
    """Регистрирует другое приложение как SINGLETON компонент."""
    # Создаем обертку Component вокруг BaseApp
    wrapper = AppComponentWrapper(app)
    self._container.register_app(name, wrapper)
```

### **Шаг 3: Обновить ComponentDescriptor**

- Для SINGLETON: оставить как есть (через `get_component`)
- Для REQUEST: можно добавить предупреждение или требовать явный `req.get()`

---

## 📊 Сравнительная таблица

| Критерий                  | Текущая реализация    | Альтернативная реализация           |
| ------------------------- | --------------------- | ----------------------------------- |
| **Простановка app**       | ❌ Только SINGLETON   | ✅ Все компоненты                   |
| **Авто-старт REQUEST**    | ❌ Fire-and-forget    | ✅ Явный `await req.get()`          |
| **Правила зависимостей**  | ❌ Слишком жесткие    | ✅ Корректные                       |
| **Инъекция зависимостей** | ❌ Непоследовательная | ✅ Всегда `.obj`                    |
| **Порядок остановки**     | ❌ Произвольный       | ✅ Обратный порядок                 |
| **Наследование**          | ❌ Не поддерживается  | ✅ Через MRO                        |
| **Инверсия зависимостей** | ❌ Жесткая связка     | ✅ Через scope_getter               |
| **RequestContext**        | ❌ Не реализован      | ⚠️ Нужно создать                    |
| **Биндинг приложений**    | ❌ Не реализован      | ⚠️ Нужно добавить                   |
| **API**                   | ⚠️ Неочевидно         | ✅ Явный (`req.get()`, `req.use()`) |

---

## ✅ Заключение

**Альтернативный вариант существенно лучше** и решает все проблемы из комментариев.

**Рекомендация:** Перейти на альтернативную реализацию и доработать:

1. Создать `RequestContext`
2. Добавить биндинг приложений
3. Обновить документацию и примеры
